//
//  Version.swift
//  Embassy
//

/// Package-level metadata. SwiftPM targets have no bundle to read a version from,
/// so this is the single place the release number lives; bump it when tagging.
public enum Embassy {
    public static let version = "5.0.0"
}
